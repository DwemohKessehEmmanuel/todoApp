{
  "name": "pern_try_1",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd pern_auth_1 & npm install)",
    "server":"node ./server/server.js",
    "client":"cd pern_auth_1 && npm start" 
  },
  "keywords": [],
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
